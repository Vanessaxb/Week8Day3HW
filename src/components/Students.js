import Score from "./Score"

export default function Students(props) {
    // const studentElements = students ? (
       
    // )
    return (
        <div>
            <Students.name />
            <Students.bio />
            <Score />
        </div>
    )
}